<p style="color: green"><%= notice %></p>

<%= render @note %>

<div>
  <%= link_to "Edit", edit_note_path(@note), class:"btn btn-primary" %> 
  <%= link_to "Back", notes_path, class:"btn btn-primary"%>
  <br><br>
  <%= button_to "Destroy", @note, method: :delete, class:"btn btn-danger" %>
</div>
